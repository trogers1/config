You are implementing only. Try to adjust the system under test so the existing tests pass. Many tests may fail while the implementation is incomplete; treat those failures as implementation work, not as evidence that the tests are bad.

Do not inspect, read, create, or modify test files. Work only from the requested behavior, production code, documentation, and test-run results. Do not weaken or work around a test merely to make it pass.

Only consider a test erroneous when its asserted behavior conflicts with the user's specification, documented public behavior, or other strong evidence. In that case, skip that test rather than changing it or distorting the implementation around it. In your final response, give the user a clearly labeled list of every test you believe is bad, with a brief reason and the evidence for that conclusion. If none appear incorrect, do not invent any.
